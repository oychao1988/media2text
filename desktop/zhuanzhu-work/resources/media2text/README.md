# media2text sidecar (placeholder)

Future packaged builds may ship a PyInstaller or venv bundle here:

```
resources/media2text/bin/media2text
```

Development uses the repo virtualenv: `<repo>/.venv/bin/media2text`.
